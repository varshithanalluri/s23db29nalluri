extends layout

block content
  h2 Grid Build page
  if query
    p Rows: #{query.rows}
    p Columns: #{query.cols}
    table#outer
      - for (let i = 1; i <= query.rows; i++)
        tr#middle
          - for (let j = 1; j <= query.cols; j++)
            td=calculateCellValue(i, j)
  else
    h3 Invalid Input
    p Please provide valid rows and columns (1 to 10).

  script.
    function calculateCellValue(row, col) {
      if (row === col) {
        return 1;
      } else if (row === col + 1) {
        return 2;
      } else if (row === col - 1) {
        return row;
      } else {
        return 0;
      }